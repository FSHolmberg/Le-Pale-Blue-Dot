


class JB:
    def __init__(self, prompt: str | None = None) -> None:
        self.prompt = prompt
        
    def respond(self, text: str) -> str:
        return "yes, JB is here and still as unimpressed."
